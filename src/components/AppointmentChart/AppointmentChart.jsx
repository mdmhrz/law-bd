import React from 'react';
import { LineChart } from 'recharts';

const AppointmentChart = () => {
    return (
        <div>
            <LineChart width={500} height={600}></LineChart>
        </div>
    );
};

export default AppointmentChart;